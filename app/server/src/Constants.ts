export const CLIENT = 'api';

export enum Types {
    CREATE = 'create',
    UPDATE = 'update',
    DELETE = 'delete'
}

export enum EventSource {
    ACCOUNTS = 'account',
    MONTH = 'month',
    TRANSACTION = 'transaction'
}